using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class UIManager : MonoBehaviour
{
    /// <summary>
    /// Disable other UIs when one is active for optimisation
    /// </summary>
    public Button gameStartConfirmation;
    public Button chooseSongConfirmation;
    public Button choseSettingsConfirmation;
 
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
